# Path Resolution Configuration for Smart Connections Plugin
# 
# This file contains optional environment variables that can be used to override
# the default path resolution behavior. Copy this file to .env and uncomment
# the variables you want to customize.

# Optional: Override dependency paths for development
# Uncomment and modify these paths to point to your local development copies
# JSBRAINS_PATH=/path/to/local/jsbrains
# OBSIDIAN_SMART_ENV_PATH=/path/to/local/obsidian-smart-env
# SMART_CHAT_OBSIDIAN_PATH=/path/to/local/smart-chat-obsidian
# SMART_CONTEXT_OBSIDIAN_PATH=/path/to/local/smart-context-obsidian
# SMART_PLUGINS_OBSIDIAN_PATH=/path/to/local/smart-plugins-obsidian

# Optional: Specify plugin installation path
# This is useful if the plugin is installed in a non-standard location
# OBSIDIAN_PLUGIN_PATH=/path/to/vault/.obsidian/plugins/smart-connections

# Debug Options
# Uncomment to enable debug logging for path resolution
# DEBUG_PATH_RESOLUTION=true

# Development Mode
# Uncomment to enable development mode features
# NODE_ENV=development

# Cross-Platform Compatibility
# These variables are automatically detected but can be overridden if needed
# PLATFORM=win32|darwin|linux
# PATH_SEPARATOR=\|/

# Build Configuration
# Override build output directories if needed
# DIST_DIR=dist
# BUILD_TARGET=node16

# Testing Configuration
# Override test data paths for testing
# TEST_DATA_PATH=/path/to/test-data
# MOCK_FS_ENABLED=true